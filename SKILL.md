---
name: alcohol-route-planner
description: 为城市夜生活步行聚会生成三种便利店 checkpoint 路线方案，并在用户选择后输出一张 9:16 最终路线图。
---

# Alcohol Route Planner

## 数据源与安全

- 正式运行必须使用高德地图 Web 服务 API 获取地理编码、步行路径、周边 POI 和便利店数据。
- API Key 只从本机环境变量 `AMAP_API_KEY` 读取；绝不要求用户在聊天中发送 Key。
- 禁止把 Key 写入 `SKILL.md`、JSON、Prompt、图片、Git 提交或 GitHub 仓库。
- 缺少 Key 时，明确提示用户先配置本机环境变量，不得用模型想象真实店铺、坐标或路线。
- 图片中的地图、道路、便利店和坐标必须来自高德返回结果；无法验证的内容标记为近似示意。

## 路径规划硬规则

- 先获取起点到终点的高德真实步行主路线，再在主路线走廊内寻找 checkpoint；不得先收集一批店再凭直觉连线。
- 从起点 A 到终点 B 必须沿总体前进方向推进，不得在已走过的道路或路线段上折返。
- 允许为了接近目标便利店数量而向左右两侧顺路绕行，但绕行后必须继续朝终点推进。
- 每一条候选路线都必须逐段调用高德步行 API 验证：起点→店1→店2→…→终点；任何未验证的店不得进入最终图。
- 以候选点到主路线的步行偏离距离和加入该点后的总路程增量判断“顺路”；不能只用经纬度直线距离。
- 便利店数量是软目标：在不明显绕路的前提下尽量达到用户目标；若真实 POI 密度不足，明确说明实际数量并保留可选 checkpoint。
- 路线距离和时间先满足用户输入，再在该约束内优化便利店数量、地标和导航易用性。

## 固定交互流程

严格按以下顺序执行：

1. 先询问使用者：城市、行政区、期望活动片区/夜生活区域、人数范围、最长距离或步行时间、目标便利店数量。
2. 可选询问起点、终点、环线/单向偏好、夜生活优先级、避开区域或品牌。未提供时合理推断。
3. 检查 `AMAP_API_KEY` 是否存在；存在后调用高德 API 核验地点、步行路线和 POI。
4. 根据高德真实路线生成一张“三方案对比图”，包含 A/B/C 三条路线、距离、预计时间、便利店数量、路线风格和选择提示。图片版式参考 `assets/three-route-reference.png`。
5. 等使用者明确选择 A、B 或 C 后，只输出所选路线的最终 9:16 路线图；不得提前跳过选择步骤。

## 路线与图片要求

- 三方案固定为：A 观光/CBD 夜景线、B 挑战/密集线、C 主干道/易导航线；应结合当地地标重新命名。
- 最终图片沿用案例的地图占比、右侧便利店清单、路线亮点、难度、注意事项和底部品牌图例布局。
- 地图必须包含起点、终点、北向、主要道路、地标、路线连线、编号 checkpoint 和便利店标识；便利店标识使用小型概略图标即可。
- 近似模式下不得声称实时营业、精确坐标或真实店铺状态；图片中保留“实际道路、店铺与营业状态以高德地图为准”。
- 便利店是可选 checkpoint，不把饮酒量作为优化目标；提示步行、公共交通或代驾，适量饮酒，安全第一。

## 默认输出

用户选择路线后，生成一张单路线 9:16 PNG，并在回复中简短说明已生成。若无法确认路线真实性，使用“示意/近似”措辞。

详细字段和案例见：
- `references/workflow.md`
- `docs/PRODUCT_SPEC.md`
- `docs/ROUTE_ALGORITHM.md`
- `schemas/input.schema.json`、`schemas/output.schema.json`
